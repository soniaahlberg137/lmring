/**
 * Arena configuration constants
 */

export const MAX_COMPARISON_CARDS = 5;
export const MIN_COMPARISON_CARDS = 1;
export const DEFAULT_COMPARISON_CARDS = 2;

// Card width constraints
export const CARD_MIN_WIDTH = 320;
