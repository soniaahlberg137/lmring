# Path to the hal-harness repo (relative to the sidecar/ directory)
HAL_DIR=../hal-harness

# Where lmring is running (the benchmark-runs callback endpoint)
LMRING_CALLBACK_URL=http://localhost:3000/api/benchmark-runs

# Shared secret — must match TESSERA_SIDECAR_SECRET in lmring's .env
TESSERA_SIDECAR_SECRET=7d08f3d2f874f1fb827a7bd9231cd6a79903ab09f02db3ac65f247e0a701e2b0

# Optional: full path to hal-eval if it's not on PATH
# HAL_EVAL_CMD=/Users/sonia/IdeaProjects/hal-harness/.venv/bin/hal-eval
